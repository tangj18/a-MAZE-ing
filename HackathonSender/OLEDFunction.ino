void bunnyjump() {
  display.drawBitmap(130, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(125, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(120, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(115, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(110, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(105, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(100, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(95, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(90, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(85, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(80, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(75, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(70, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(65, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(60, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(55, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(45, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(40, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(35, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(30, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(25, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(20, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(15, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(10, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(5, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(0, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-5, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-10, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-15, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-20, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-25, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-30, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-35, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-40, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-45, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-50, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-55, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-60, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-65, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-70, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-75, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-80, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-85, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-90, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-95, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-100, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-105, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-110, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-115, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-120, 1, bunny3, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-125, 1, bunny1, 128, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(-130, 0, bunny2, 128, 32, WHITE);
  bufferdisplay();
}

void angeryface1() {
  display.drawBitmap(50, 1, angery1, 32, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, 0, angery1, 32, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -1, angery1, 32, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, 0, angery1, 32, 32, WHITE);
  bufferdisplay();
}

void angeryface2() {
  display.drawBitmap(50, 0, angery2, 30, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -1, angery2, 30, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -2, angery2, 30, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -3, angery2, 30, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -2, angery2, 30, 32, WHITE);
  bufferdisplay();
}

void angeryface3() {
  display.drawBitmap(50, 0, angery3, 33, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -1, angery3, 33, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -2, angery3, 33, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -3, angery3, 33, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -4, angery3, 33, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -3, angery3, 33, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -2, angery3, 33, 32, WHITE);
  bufferdisplay();
  display.drawBitmap(50, -1, angery3, 33, 32, WHITE);
  bufferdisplay();
}
